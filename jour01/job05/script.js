function afficherjourssemaines() {
    // Déclare un tableau contenant les jours de la semaine
    var jourssemaines = ["Lundi", "Mardi", "Mercredi", "Jeudi", "Vendredi", "Samedi", "Dimanche"];

    // Utilisation d'une boucle for pour parcourir le tableau et afficher chaque jour
    for (var i = 0; i < jourssemaines.length; i++) {
        console.log(jourssemaines[i]); // Affichage dans la console
    }
}
